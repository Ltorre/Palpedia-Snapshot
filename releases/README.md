# Release notes

Before creating a release tag, add `releases/vX.Y.Z.md` to the same commit.

Keep each note useful as a standalone reference: state what changed, what the executable produces, how to run it, and any compatibility or safety limits. The release workflow refuses to publish a tag that has no matching note file.
